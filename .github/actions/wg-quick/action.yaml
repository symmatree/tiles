name: 'wg-quick'
description: 'Connect to a Wireguard VPN'
inputs:
  wg_config:
    description: 'Wireguard config'
    required: true
  conf_dir:
    description: 'Directory to write the Wireguard config file to (suggested: runner.temp)'
    required: true
outputs:
  wg_connected:
    description: 'Whether the Wireguard VPN is connected'
    value: ${{ steps.wireguard.outputs.wg_connected }}
runs:
  using: "composite"
  steps:
      - name: WG Quick
        id: wireguard
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${{ inputs.wg_config }}" ]; then
            echo "No Wireguard config provided"
            exit 1
          fi
          echo "Setting up Wireguard..."
          time sudo apt-get -y \
           -qq -o Dpkg::Progress-Fancy="0" -o APT::Color="0" -o Dpkg::Use-Pty="0" \
            install wireguard-tools
          echo "${{ inputs.wg_config }}" > "${{ inputs.conf_dir }}/wg0.conf"
          chmod 0600 "${{ inputs.conf_dir }}/wg0.conf"

          echo "=== Config file contents (base64 encoded, keys removed) ==="
          grep -v -E "^(PrivateKey|PublicKey)\s*=" "${{ inputs.conf_dir }}/wg0.conf" | base64 | base64

          echo "Connecting..."
          time sudo wg-quick up "${{ inputs.conf_dir }}/wg0.conf"
          echo "Connected"
          echo "wg_connected=1" | tee -a "$GITHUB_OUTPUT"

          conf="${{ inputs.conf_dir }}/wg0.conf"
          dns_server=$(grep -E '^DNS\s*=' "${conf}" | head -1 | sed 's/^DNS\s*=\s*//' | tr -d ' ' | cut -d, -f1)
          dns_server="${dns_server:-10.1.0.1}"

          echo "=== WireGuard diagnostics ==="
          sudo wg show wg0 || true

          handshake=$(sudo wg show wg0 latest-handshakes 2>/dev/null | awk '{print $2}' | head -1)
          if [ -z "${handshake}" ] || [ "${handshake}" = "0" ]; then
            echo "::error title=WireGuard::No handshake with peer (tunnel not established)"
            exit 1
          fi
          handshake_age=$(( $(date +%s) - handshake ))
          echo "Latest handshake: ${handshake_age}s ago"

          echo "Pinging VPN DNS ${dns_server}..."
          if ! ping -c1 -W3 "${dns_server}"; then
            echo "::error title=WireGuard::VPN DNS ${dns_server} unreachable"
            exit 1
          fi

          echo "Resolving storage.googleapis.com (Terraform GCS backend)..."
          if ! getent hosts storage.googleapis.com; then
            echo "::error title=WireGuard::Cannot resolve storage.googleapis.com with VPN DNS active"
            exit 1
          fi

          echo "Resolving GitHub Actions OIDC host..."
          if ! getent hosts run-actions-2-azure-eastus.actions.githubusercontent.com \
            && ! getent hosts actions.githubusercontent.com; then
            echo "::error title=WireGuard::Cannot resolve GitHub Actions OIDC host with VPN DNS active"
            exit 1
          fi
