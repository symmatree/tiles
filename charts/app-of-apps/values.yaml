# Values propagated into the root Application's valuesObject, and from there to
# every child Application. Terraform supplies the real values from
# module.cluster.app_of_apps_values; these placeholders exist so `helm template`
# renders offline for build.sh, and so the propagated set is declared in one
# place (scripts/helm-common.bash reads the keys from here).
targetRevision: placeholder
pod_cidr: placeholder
cluster_name: placeholder
external_ip_cidr: placeholder
ingress_lb_ip: placeholder
vault_name: placeholder
project_id: placeholder
seed_project_id: placeholder
cluster_nfs_path: placeholder
datasets_nfs_path: placeholder
nfs_server: placeholder
