apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: jupyterhub-oauth2-proxy
  namespace: "{{ .Release.Namespace }}"
type: Opaque
spec:
  itemPath: 'vaults/{{ required ".Values.vault_name is required" .Values.vault_name }}/items/jupyterhub-oauth2-proxy'
