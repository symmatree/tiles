# Chart defaults live in charts/cilium/values.yaml. Templated keys in charts/cilium/application.yaml valuesObject.
cilium:
  resources:
    requests:
      memory: 160Mi
  envoy:
    resources:
      requests:
        memory: 25Mi
